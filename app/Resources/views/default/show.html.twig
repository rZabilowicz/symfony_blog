{% extends 'base.html.twig' %}

{% block body %}
    <h1>{{ post.title }}</h1>
    <div>
        <p>{{ post.lead }}</p>
        <p>{{ post.content }}</p>

        <span class="float-right">Added: {{ post.createdAt | date('H:i d/m/Y')}}</span>
        
        <br>
        <hr>

        <h3>Comments</h3>
    </div>   
{% endblock %}
